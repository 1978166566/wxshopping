<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cssl.dao.FlatMapper">
    <!--品牌查询-->
    <select id="selBand" resultType="flat">
          SELECT * FROM `flat` WHERE `oc_id`=#{oc_id} LIMIT 6
    </select>

</mapper>